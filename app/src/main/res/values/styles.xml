<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="android:fontFamily">@font/pacifico</item>
        <item name="colorPrimary">@color/newcolor</item>
        <item name="colorPrimaryDark">@color/newcolor1</item>
        <item name="colorAccent">@color/newcolor2</item>

    </style>

    <style parent="Theme.AppCompat.Light.NoActionBar" name="AppTheme.NoActionBar">
        <item name="android:fontFamily">@font/pacifico</item>
    <item name="android:windowIsTranslucent">true</item>

    <item name="android:windowNoTitle">true</item>

    <item name="android:windowFullscreen">true</item>

</style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="android:fontFamily">@font/pacifico</item>
    </style>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:fontFamily">@font/pacifico</item>
    </style>
    <!-- Base application theme. -->
</resources>
